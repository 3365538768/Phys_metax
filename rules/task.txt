数据集：auto_output/dataset_400
任务：
测试不同模型下在训练集上训练，在test上做测试的效果。对于下面提到的模型构建，用参数的方式选择用哪种模型训练和推理。对于checkpoint和eval的保存也需要按照下面的序号方式用子目录保存。
对于模型构建：
1.先只考虑利用输入的时序图像，通过image encoder喂给transformer，再通过物理参数预测头和动作预测头得到结果
2.在1的基础上考虑将物理预测头设置为多个独立的 MLP，各预测一个物理标量（实现为 --arch 2）
