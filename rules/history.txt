[2026-03-20] train / eval：`--dataset_dir`（默认 `dataset_400`）指定相对 `auto_output` 的扁平数据集根或绝对路径；checkpoint `train_hparams` 记录 `dataset_dir` / `dataset_root_resolved`。

[2026-03-20] train：checkpoint 文件名带配置标签（如 `epoch_N__T{num_frames}_H{img_size}_bs..._lr..._ep..._seed..._ds400.pt`、`final__....pt`），`.pt` 内增加 `train_hparams`；仍复制 `final.pt` 供默认识别；`epoch_time_log` 与训练曲线图文件名同步带标签。

[2026-03-20] 已按 rules/task.txt 落实以下内容：

1) 数据：my_model 支持 --data_layout dataset_400，从 auto_output/dataset_400/train 训练、eval 时使用 dataset_400/test（与 task 中铝罐测试集划分一致）。保留 legacy：原 auto_output/<action>/... + 7:3 随机划分。

2) 模型架构（--arch）：
   - arch=1：时序图 → ResNet18 → Transformer → 单一物理 MLP（4 维）+ 动作头（与原先一致）。
   - arch=2：在 arch=1 基础上，物理量改为 4 个独立小 MLP 各预测一个标量，再经 softplus/sigmoid 约束。

3) 产物路径按序号分子目录：
   - checkpoint：my_model/checkpoints/<arch>/final.pt（及可选 epoch_*.pt），文件内保存 arch、data_layout、model_state_dict。
   - TensorBoard：my_model/runs/<arch>/
   - 训练 loss 曲线：my_model/visualizations/<arch>/train_loss.png
   - eval 图表与 metrics.json：my_model/visualizations/<arch>/<YYYYMMDD_HHMMSS>/

4) eval 默认 checkpoint 为 my_model/checkpoints/<arch>/final.pt；若 ckpt 内带 arch 字段则自动对齐模型结构。

待办（需在本机 GPU 上跑实验）：分别训练 arch=1 与 arch=2，对比 test 上 metrics.json / 可视化。

[2026-03-20] Dataset400：修复 __getitem__ 在读取失败时用「下一个样本」顶替当前 idx，导致图像与 gt 错配；改为构建时 preflight（完整读帧+resize）剔除坏样本；新增 --no_dataset_preflight 关闭预检。

[2026-03-20] Dataset400：gt.json 中 regression.yield_stress 为 null 时 float(None) 导致预检误判为 PNG 损坏；已用 _regression_float 将 null 视为 0.0（与训练头 softplus 一致）。

[2026-03-20] train：每个 epoch 用 perf_counter 计时；终端打印秒/分钟；TensorBoard `train/epoch_seconds`；`visualizations/<arch>/train_epoch_seconds.png`；`checkpoints/<arch>/epoch_time_log.json`。

[2026-03-20] train DDP：说明多卡与单卡耗时接近多为 DataLoader 读盘瓶颈（原 4 进程×4 workers）；默认 DDP 时 num_workers=2、persistent_workers；启动打印 world_size/步数/等效 batch；DDP 下 loss 做 all_reduce 再打印（原仅 rank0 子集均值）。

[2026-03-20] train：新增 `--amp_bf16`，前向+loss 在 `torch.autocast(device_type="cuda", dtype=torch.bfloat16)` 下计算；不支持则自动关闭；ckpt 记录 `amp_bf16`。
