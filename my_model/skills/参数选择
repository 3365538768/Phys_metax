# 参数选择技能规范

## 目标
- 建立一套可解释的超参数选择规则，避免盲目试参。

## 适用场景
- 新任务初始配置。
- 训练不收敛、过拟合、显存不足等问题排查。
- 分阶段训练权重重设。

## 参数分层
- 一层（结构参数）：`embed_dim, depth, num_heads, patch/tubelet`。
- 二层（训练参数）：`lr, weight_decay, batch_size, warmup, epochs`。
- 三层（损失参数）：多任务 loss 权重、阶段权重。
- 四层（系统参数）：`num_workers, amp, grad_clip, ddp`。

## 选择流程
1. 先定资源边界：GPU 显存、训练时长、可接受吞吐。
2. 先保稳定再提性能：先让 loss 下降稳定，再扩大模型规模。
3. 学习率与 batch 联动：`effective_batch` 变化时按比例调 `lr`。
4. 多任务权重从“主任务优先”开始，再逐步抬升辅助任务。
5. 分阶段训练中，每次只改一类参数，避免耦合干扰。

## 默认建议（起步）
- 优化器：`AdamW`
- `lr`: `1e-4`（回归+场预测可从 `5e-5` 到 `2e-4` 网格）
- `weight_decay`: `0.01`
- warmup: 总步数 `3%-5%`
- 梯度裁剪：`1.0`
- 混合精度：开启（若数值不稳可关闭定位）

## 调参判据
- 若 train/val 同时高：模型容量或输入信息不足。
- 若 train 低、val 高：过拟合，增广/正则或减小模型。
- 若 loss 振荡：降 `lr`、加 warmup、检查异常样本。
- 若单任务长期不降：提升该任务权重或单独预训练该 head。

## 输出要求
- 每次试验必须记录：改动项、原因、结果、下一步动作。
- 结果写入统一实验表（至少包含 seed、配置路径、最佳指标）。
