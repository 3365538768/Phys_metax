# 评估设计技能规范

## 目标
- 对每个阶段输出可量化结论，避免“只看总 loss”。

## 适用场景
- 分阶段训练（stage1/stage2/stage3/stage4/joint）评估。
- 多任务模型回归/场预测/方向预测联合验收。

## 指标体系
- 参数回归：`MAE, RMSE, R2`（按 `E/nu/density/yield_stress` 分项统计）。
- 场预测：`L1, MSE, PSNR/SSIM`（可选）。
- 方向预测：`cosine similarity` 与角度误差（度）。
- 分布一致性：预测与 GT 的直方图对比（含 log 轴指标项）。

## 评估流程
1. 固定评估集与随机种子。
2. 加载对应 stage 最优权重。
3. 输出逐样本结果（便于失败样本追踪）。
4. 聚合为总体报告 + 分桶报告（按材料或参数区间）。
5. 保存可视化（曲线、散点、误差分布、典型案例）。

## 多 GPU 推理规范
- 使用 DDP 或分卡切分样本，禁止重复评估同一样本。
- 汇总前做去重，按样本 ID 对齐。
- 多卡与单卡结果差异应在可接受浮动内（给出阈值）。

## 质量门禁
- 每次评估都输出机器可读报告：`eval_metrics.json`。
- 报告必须包含版本信息：模型配置、权重路径、数据版本、代码 commit（若可用）。
- 至少保存 top-k 最差样本列表及原因提示。

## 常见错误与规避
- 错误：只报平均指标，掩盖长尾失败。  
  规避：增加 P50/P90/P95 和最差样本可视化。
- 错误：训练集和评估集泄漏。  
  规避：评估前强制检查样本 ID 交集。
