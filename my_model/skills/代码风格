# 代码风格技能规范

## 目标
- 保持代码可读、可维护、可协作，减少隐式约定与技术债。

## 通用规则
- Python 版本兼容项目当前环境（如 3.9）。
- 命名语义化：变量名体现物理含义与张量维度。
- 函数单一职责，超过 80 行优先拆分。
- 配置优先：可调参数不硬编码。
- 关键分支加简短注释解释“为什么”，不是“做了什么”。

## 张量与接口规则
- 显式标注 shape 约定（docstring 或注释）。
- 模型 forward 输入输出使用字典键名统一，不随阶段变化。
- 避免在深层函数里隐式改设备或 dtype。
- 禁止静默吞错；异常信息要包含样本路径/键名。

## 工程规则
- 新增模块必须附最小 smoke test。
- I/O 与核心算法分离，便于测试和复用。
- 日志使用统一前缀，便于 `rg` 检索。
- 兼容 Linux 路径与无 GUI 环境。

## 提交前检查清单
- [ ] `py_compile` 或等价语法检查通过
- [ ] 新增配置项已在主 config 注释说明
- [ ] 不引入无用依赖
- [ ] 不破坏已有数据格式兼容性

因为我现在要进行不同架构的模型测试，对于每一种设计的模型，单独创建一个model文件，其他部分复用