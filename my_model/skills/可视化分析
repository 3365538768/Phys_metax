# 可视化分析技能规范

## 目标
- 用最少图表回答关键问题：模型学到了什么、哪里错、为什么错。

## 适用场景
- 数据集质量审查。
- 训练过程监控。
- 评估结果复盘与失败案例定位。

## 必备图表
- 参数分布直方图：`E, nu, density, yield_stress, hardening, softening`。
- 训练曲线：总 loss + 分任务 loss。
- 预测对比：GT vs Pred 散点图（回归任务）。
- 时序可视化：`rgb/stress/flow/force_mask` 关键帧拼图。
- 误差热力图：场预测空间误差。

## 规范
- 统一命名：`{split}_{task}_{metric}.png`。
- 统一配色：GT 与 Pred 固定颜色映射，避免跨图含义变化。
- 对数量级跨度大的指标（如 `E/yield_stress`）支持 log 轴。
- 所有图必须带单位与坐标轴标签。

## 分析流程
1. 先看数据分布是否偏斜和空洞区间。
2. 再看训练曲线是否存在过拟合或欠拟合。
3. 最后看逐样本误差，定位典型失败类别。

## 质量门禁
- 每次实验至少产出一个 `visual_report.md`，包含图和结论。
- 图表必须可追溯到实验 ID 与配置文件。
- 禁止仅给图不下结论；每张关键图至少一条结论说明。
