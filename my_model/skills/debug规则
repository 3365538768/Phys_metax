# Debug 规则技能规范

## 目标
- 以最短路径定位问题根因，避免“盲改”。

## 排查优先级（固定顺序）
1. 环境与依赖：Python/torch/cuda/扩展模块版本与可导入性。
2. 数据读取：样本路径、字段完整性、shape/dtype/device。
3. 前向过程：关键层输入输出 shape、NaN/Inf 检查。
4. 训练过程：loss 构成、梯度是否为 0、学习率是否异常。
5. 推理与导出：多卡一致性、结果落盘路径与命名。

## Debug 最小闭环
- 最小可复现命令（单样本、单卡、固定 seed）。
- 打印关键张量统计（min/max/mean/std，shape）。
- 在异常附近增加 assert 与上下文日志。
- 修复后复跑同一最小样例验证。

## 日志规范
- 每条日志包含：阶段、样本 ID、视角 ID（若有）。
- 错误日志需包含输入路径和配置摘要。
- 对批处理任务，保存 `failed_cases.json`。

## 常见问题快速规则
- `ModuleNotFoundError`：先检查运行入口的 `sys.path` 与包初始化。
- `NoneType` 标签错误：优先做数据预扫描，不在训练时临时兜底。
- 通道维错误：统一 `CTHW`/`TCHW`，仅保留一套规范。
- 单卡占满多卡空闲：检查子进程环境变量是否覆盖 `CUDA_VISIBLE_DEVICES`。

## 完成标准
- 能明确给出“根因 + 证据 + 修复点 + 回归验证结果”四项结论。
