# 数据集构建技能规范

## 目标
- 保证数据可复现、可追踪、可直接训练，避免训练时才暴露脏样本问题。

## 适用场景
- 新数据集落地。
- 新增字段（如 `force_mask`）或改张量格式。
- 清理历史数据兼容逻辑。

## 数据规范
数据集格式参考dataset_defomation_Stress_500_new，核心为仿真下多视角的RGB渲染图、驱动受力区域mask（用红色标出）、应力场、形变场（不同方向速度用不同颜色表示）、材料参数（类型, E, nu, density, yield_stress为核心）、动作形式
## 构建流程（必须按顺序）
1. 数据扫描：收集样本路径并检查必需文件是否存在。
2. 标签校验：校验回归字段非空且可转 `float`。
3. 张量归一化：统一为 `[C,T,H,W]`。
4. 样本过滤：坏样本直接跳过并写入 `invalid_samples.log`。
5. 统计输出：保存总样本数、有效样本数、各参数分布摘要。

## 质量门禁
- `__len__` 与有效样本列表一致。
- `__getitem__` 不允许出现随机 `KeyError/NoneType`。
- 支持 `num_workers>0` 的 DataLoader 稳定读取。
- 首次构建后保存缓存索引（可选），二次启动加速。

## 常见错误与规避
- 错误：训练时才发现 `yield_stress=null`。  
  规避：初始化阶段预扫描并过滤。
- 错误：字段名不一致（如 `gt_parameters.json` 与 `gt.json` 并存冲突）。  
  规避：保留单一真值源，统一读 `gt.json`。
- 错误：`force_mask` 与 `rgb` 帧数不对齐。  
  规避：打包阶段强制对齐并记录修正策略。

## 交付物模板
- `dataset_*.py`: 数据读取与校验逻辑。
- `scripts/check_dataset.py`: 离线检查脚本。
- `dataset_report.json`: 数据健康报告。
